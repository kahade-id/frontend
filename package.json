{
  "name": "kahade-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev:landing": "pnpm --filter landing dev",
    "dev:app": "pnpm --filter app dev",
    "dev:admin": "pnpm --filter admin dev",

    "build:landing": "pnpm --filter landing build",
    "build:app": "pnpm --filter app build",
    "build:admin": "pnpm --filter admin build",

    "build": "pnpm run build:landing && pnpm run build:app && pnpm run build:admin",

    "preview:landing": "pnpm --filter landing preview",
    "preview:app": "pnpm --filter app preview",
    "preview:admin": "pnpm --filter admin preview"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
